{
  "version": 2,
  "builds": [
    { "src": "Interfaz/package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/Interfaz/$1" }
  ]
}